package com.embeltech.meterreading.extensions

/*
 * Add Maintenance Dialog
 */
/*
inline fun Activity.showAddMaintenanceAlertDialog(func: AddMaintenancesDialogHelper.() -> Unit): AlertDialog =
        AddMaintenancesDialogHelper(this).apply {
            func()
        }.create()

inline fun Fragment.showAddMaintenanceAlertDialog(func: AddMaintenancesDialogHelper.() -> Unit): AlertDialog =
        AddMaintenancesDialogHelper(this.context!!).apply {
            func()
        }.create()


*/
/*
 * POI Dialog
 *//*

inline fun Activity.showPOIAlertDialog(func: POIDialogHelper.() -> Unit): AlertDialog =
        POIDialogHelper(this).apply {
            func()
        }.create()

inline fun Fragment.showPOIAlertDialog(func: POIDialogHelper.() -> Unit): AlertDialog =
        POIDialogHelper(this.context!!).apply {
            func()
        }.create()


inline fun Activity.showAddVehicleSuccessAlertDialog(func: VehicleAddedDialog.() -> Unit): AlertDialog =
        VehicleAddedDialog(this).apply {
            func()
        }.create()*/
